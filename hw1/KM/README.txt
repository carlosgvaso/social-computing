KM
==

Run
---

1. Place the input file into the same folder as the KM.jar file.

2. From the same directory as the KM.jar file run tho following command, where <input filename> should be replaced by the input filename:

    $ java -jar KM.jar <input file name>

